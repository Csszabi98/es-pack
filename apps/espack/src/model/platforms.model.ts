/**
 * Enumeration of platform options provided by esbuild
 * @see https://esbuild.github.io/api/#platform
 */
export enum Platforms {
    NODE = 'node',
    BROWSER = 'browser',
    NEUTRAL = 'neutral'
}
